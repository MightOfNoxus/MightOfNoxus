server:
  port: 8080

spring:
  application:
    name: agent-config-platform
  ai:
    dashscope:
      api-key: ${AI_DASHSCOPE_API_KEY:}

management:
  endpoints:
    web:
      exposure:
        include: health,info
